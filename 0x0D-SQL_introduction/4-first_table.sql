-- creates a table called first_table in the current database in mySQL server
-- contains an INT id and a VARCHAR name
CREATE TABLE IF NOT EXISTS first_table(
        id INT,
        name VARCHAR(256)
);
